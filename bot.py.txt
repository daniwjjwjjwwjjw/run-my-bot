from telegram.ext import Application, CommandHandler, CallbackQueryHandler, MessageHandler, filters
from telegram import InlineKeyboardButton, InlineKeyboardMarkup
import os

TOKEN = os.environ.get("TELEGRAM_TOKEN")

async def start(update, context):
    keyboard = [
        [InlineKeyboardButton("👥 Community", url="https://t.me/Gift_Empire_play")],
        [InlineKeyboardButton("🛠 Support", url="https://t.me/mpiresupport")],
        [InlineKeyboardButton("🎮 Start", callback_data="start_game")]
    ]
    await update.message.reply_text("🎮 Welcome to the bot! Press Start to begin!", reply_markup=InlineKeyboardMarkup(keyboard))

async def button(update, context):
    query = update.callback_query
    await query.answer()
    await query.edit_message_text("🎮 Game started!")

async def auto(update, context):
    if update.message.chat.type == "private":
        await start(update, context)

if name == "main":
    app = Application.builder().token(TOKEN).build()
    app.add_handler(CommandHandler("start", start))
    app.add_handler(CallbackQueryHandler(button))
    app.add_handler(MessageHandler(filters.TEXT & filters.ChatType.PRIVATE, auto))
    
    print("🤖 ربات روشن شد!")
    app.run_polling()